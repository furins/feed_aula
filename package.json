{
  "name": "feed-aula",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "web:build": "node scripts/build-web.mjs",
    "desktop:dev": "tauri dev --no-watch",
    "desktop:dev:watch": "tauri dev",
    "desktop:build": "tauri build"
  },
  "dependencies": {
    "js-aruco2": "2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}