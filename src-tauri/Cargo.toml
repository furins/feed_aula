[package]
name = "feed_aula"
version = "0.1.0"
description = "FEED - feedback formativo in aula"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
